import os
import json
import requests
import sqlite3
from fastapi import FastAPI
name = "ISMAIL"
count = 10
async def fetch_data():
    return await get_data()
def greet(user):
    return "Hello " + user
class AI:
    def run(self):
        return greet(name)
try:
    value = 10 / 0
except ValueError:
    pass
raise RuntimeError("test")
